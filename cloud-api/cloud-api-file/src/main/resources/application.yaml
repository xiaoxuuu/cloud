spring:
  config:
    import:
      - optional:api.yaml
      - optional:logging.yaml
  application:
    name: file
  profiles:
    active: ${cloud.profiles.active:local}
server:
  port: 10002
  forward-headers-strategy: framework

# 启动类输出配置
app:
  version: 1.0