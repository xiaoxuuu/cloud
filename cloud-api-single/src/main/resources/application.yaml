spring:
  config:
    import:
      - optional:cloud.yaml
      - optional:logging.yaml
      - optional:file-server.yaml
  application:
    name: single
  profiles:
    active: local
server:
  port: 9999
  forward-headers-strategy: framework